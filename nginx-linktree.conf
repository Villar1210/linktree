server {
    listen 80;
    server_name linktree.ivillar.com.br;

    access_log /var/log/nginx/linktree_access.log;
    error_log /var/log/nginx/linktree_error.log;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
        client_max_body_size 10M;
        add_header X-Frame-Options "SAMEORIGIN";
        add_header X-Content-Type-Options "nosniff";
    }

    location /static {
        alias /var/www/linktree/static;
        expires 1y;
        add_header Cache-Control "public, immutable";
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
    
    # Redirect para HTTPS (descomente após configurar SSL)
    # return 301 https://$server_name$request_uri;
}

# Configuração HTTPS (descomente após obter certificado SSL)
# server {
#     listen 443 ssl http2;
#     server_name linktree.ivillar.com.br;
#     
#     # Certificados SSL (Let's Encrypt)
#     ssl_certificate /etc/letsencrypt/live/linktree.ivillar.com.br/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/linktree.ivillar.com.br/privkey.pem;
#     
#     # Configurações SSL modernas
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#     ssl_stapling on;
#     ssl_stapling_verify on;
#     
#     # Headers de segurança
#     add_header Strict-Transport-Security "max-age=63072000" always;
#     add_header X-Frame-Options DENY;
#     add_header X-Content-Type-Options nosniff;
#     add_header X-XSS-Protection "1; mode=block";
#     
#     # Logs
#     access_log /var/log/nginx/linktree_ssl_access.log;
#     error_log /var/log/nginx/linktree_ssl_error.log;
#     
#     # Configuração para o linktree (root do subdomínio)
#     location / {
#         proxy_pass http://127.0.0.1:5000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         
#         # Timeouts
#         proxy_connect_timeout 30s;
#         proxy_send_timeout 30s;
#         proxy_read_timeout 30s;
#     }
#     
#     # Arquivos estáticos
#     location /static {
#         alias /var/www/linktree/static;
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
# }